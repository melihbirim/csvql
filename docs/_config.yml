markdown: kramdown
url: "https://melihbirim.github.io"
baseurl: "/csvql"
title: "csvql"
description: "The analytical CSV query engine for AI agents. Run SQL on CSV files in place using about 10x less memory than a database. MCP-native, sandboxed with --root, a single 1.3 MB binary."
logo: "/logo.svg"
social:
  name: csvql
  links:
    - https://github.com/melihbirim/csvql
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-feed
